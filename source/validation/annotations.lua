
return {
    not_null = not_null,
    size = size,
    min = min,
    max = max,
    email = email,
    not_empty = not_empty,
    not_blank = not_blank,
    positive = positive,
    negative = Negative,
    digits = digits,
    null = null,
    regex = regex,
}